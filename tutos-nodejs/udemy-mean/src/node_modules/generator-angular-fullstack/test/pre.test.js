'use strict';

var _path = require('path');

var _path2 = _interopRequireDefault(_path);

var _yeomanAssert = require('yeoman-assert');

var _yeomanAssert2 = _interopRequireDefault(_yeomanAssert);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('test fixtures', function () {
  it('should have package.json in fixtures', function () {
    _yeomanAssert2.default.file([_path2.default.join(__dirname, 'fixtures/package.json')]);
  });

  it('should have .yo-rc.json in fixtures', function () {
    _yeomanAssert2.default.file([_path2.default.join(__dirname, 'fixtures/.yo-rc.json')]);
  });

  it('should have all npm packages in fixtures/node_modules', function () {
    var packageJson = require('./fixtures/package.json');
    var deps = Object.keys(packageJson.dependencies);
    deps = deps.concat(Object.keys(packageJson.devDependencies));
    deps = deps.map(function (dep) {
      return _path2.default.join(__dirname, 'fixtures', 'node_modules', dep);
    });
    _yeomanAssert2.default.file(deps);
  });
});